{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug - 模板库",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}